{% extends 'base.html.twig' %}

{% block title %}Place{% endblock %}

{% block body %}
    <h1>Place</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ place.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ place.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_index_index') }}">back to list</a>

    <a href="{{ path('app_index_edit', {'id': place.id}) }}">edit</a>

    {{ include('index/_delete_form.html.twig') }}
{% endblock %}
